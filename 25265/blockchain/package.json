{
  "name": "millets-blockchain",
  "version": "1.0.0",
  "description": "Blockchain traceability for Millets Value Chain Platform",
  "main": "index.js",
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate",
    "test": "truffle test",
    "deploy": "truffle migrate --network development"
  },
  "dependencies": {
    "web3": "^4.2.2",
    "truffle": "^5.11.5",
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "ganache-cli": "^6.12.2"
  }
}
